[package]
name = "chisel-fs"
version.workspace = true
edition.workspace = true
authors.workspace = true
license = "Apache-2.0"
publish.workspace = true

[dependencies]
anyhow = { workspace = true }
slug = "0.1"

[dev-dependencies]
tempfile = "3.10"
# We'll use process commands for git to avoid heavy native dependencies for now
